# ============================================================
# ЗАНЯТИЕ 12 | ЗАДАЧА 3
# Тема: Инкапсуляция и приватные атрибуты
# ============================================================
#
# Создай класс Wallet (кошелёк):
#
# Приватные атрибуты:
#   - __balance (float)  — баланс (нельзя изменить напрямую!)
#   - __owner   (str)    — владелец (нельзя изменить после создания!)
#   - __history (list)   — история транзакций
#
# Публичные методы:
#   - deposit(amount)    — пополнение (amount должен быть > 0)
#   - withdraw(amount)   — снятие (нельзя уйти в минус)
#   - transfer(other_wallet, amount) — перевод другому кошельку
#   - get_history()      — возвращает копию истории (не сам список!)
#   - __str__
#
# @property:
#   - balance  — только для чтения (только геттер)
#   - owner    — только для чтения
#
# История должна хранить записи вида:
#   {"type": "deposit", "amount": 500, "balance_after": 1500}
#
# Создай два кошелька, сделай несколько операций, выведи историю.
# Убедись что прямое изменение wallet.__balance вызывает ошибку.
# ============================================================
