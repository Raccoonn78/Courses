# ============================================================
# ЗАНЯТИЕ 8 | ЗАДАЧА 5
# Тема: Область видимости и рекурсия
# ============================================================
#
# Задание 1 — Рекурсия.
#   Напиши рекурсивную функцию power(base, exp),
#   которая вычисляет base в степени exp без использования **.
#   Базовый случай: exp == 0 → вернуть 1
#   Рекурсивный: base * power(base, exp - 1)
#   Протестируй: power(2, 10) → 1024, power(3, 4) → 81
#
# Задание 2 — Рекурсия.
#   Напиши рекурсивную функцию flatten(lst),
#   которая "разворачивает" вложенный список:
#   flatten([1, [2, [3, [4]], 5], 6]) → [1, 2, 3, 4, 5, 6]
#   Подсказка: проверяй isinstance(item, list)
#
# Задание 3 — Замыкание (closure).
#   Напиши функцию make_multiplier(n), которая возвращает
#   функцию умножающую на n:
#   double = make_multiplier(2)
#   triple = make_multiplier(3)
#   double(5) → 10
#   triple(5) → 15
#   Объясни в комментарии, почему внутренняя функция
#   "помнит" переменную n после завершения make_multiplier.
# ============================================================
