# ============================================================
# ЗАНЯТИЕ 9 | ЗАДАЧА 2
# Тема: Собственные исключения
# ============================================================
#
# Создай иерархию исключений для банковского приложения:
#
#   BankError (базовый, наследует Exception)
#   ├── InsufficientFundsError
#   └── InvalidAmountError
#
# Напиши функцию transfer(balance, amount):
#   - Если amount <= 0 → raise InvalidAmountError("Сумма должна быть > 0")
#   - Если amount > balance → raise InsufficientFundsError(
#         f"Недостаточно средств: баланс {balance}, запрошено {amount}")
#   - Иначе → возвращает новый баланс и выводит сообщение об успехе
#
# Протестируй все три случая в блоках try/except:
#   transfer(1000, 500)   → успех
#   transfer(1000, 2000)  → InsufficientFundsError
#   transfer(1000, -50)   → InvalidAmountError
#
# Перехватывай конкретные типы, а не просто Exception.
# ============================================================
