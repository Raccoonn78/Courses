# ============================================================
# ЗАНЯТИЕ 13 | ЗАДАЧА 2
# Тема: Переопределение методов и super()
# ============================================================
#
# Создай иерархию классов для системы уведомлений:
#
# Базовый класс Notification:
#   - __init__(title, message, priority=1)
#   - send() → "Уведомление: [title] — [message]"
#   - __str__
#
# Дочерние классы:
#
#   EmailNotification(Notification):
#     - __init__ добавляет: recipient_email
#     - Переопределяет send():
#       сначала вызывает super().send(),
#       затем добавляет: "Отправлено на email: ..."
#
#   SMSNotification(Notification):
#     - __init__ добавляет: phone_number
#     - Переопределяет send():
#       обрезает message до 160 символов если длиннее,
#       выводит: "SMS на [phone]: [message]"
#
#   PushNotification(Notification):
#     - __init__ добавляет: device_id
#     - Переопределяет send():
#       если priority >= 3 — добавляет "[СРОЧНО]" к заголовку
#       выводит: "PUSH на [device]: [title] — [message]"
#
# Создай список из 4 уведомлений разных типов.
# Вызови send() для каждого — покажи полиморфизм.
# ============================================================
