# ============================================================
# ЗАНЯТИЕ 11 | ЗАДАЧА 2
# Тема: Декоратор — логирование и валидация
# ============================================================
#
# Задание 1.
#   Напиши декоратор log_call(func), который при каждом вызове
#   функции выводит:
#     "[LOG] Вызов: имя_функции(args, kwargs)"
#     "[LOG] Вернула: результат"
#   Используй @wraps(func).
#   Примени к функции multiply(a, b) = a * b.
#
# Задание 2.
#   Напиши декоратор validate_types(*types), который проверяет,
#   что типы переданных аргументов соответствуют ожидаемым.
#   Если нет — raise TypeError с понятным сообщением.
#
#   @validate_types(int, int)
#   def add(a, b):
#       return a + b
#
#   add(1, 2)      → 3
#   add(1, "2")    → TypeError: Аргумент 2 должен быть int, получен str
#
# Задание 3.
#   Напиши декоратор retry(max_attempts=3), который при исключении
#   повторяет вызов функции до max_attempts раз.
#   Если после всех попыток ошибка — пробрасывает её дальше.
#   Выводи: "Попытка N из max_attempts..."
#   Протестируй на функции, которая падает первые 2 раза.
# ============================================================
